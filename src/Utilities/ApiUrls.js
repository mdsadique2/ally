// base url of api. used in api layer as default url for api calls
const baseUrl = `${window.location.protocol}//okrcentral.github.io`;


// end point for the api
const ApiUrls = {
    sample : `${baseUrl}/sample-okrs/db.json`,
}

export {
    ApiUrls,
    baseUrl
}